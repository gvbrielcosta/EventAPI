spring:
  application:
    name: EventAPI

  datasource:
    url: jdbc:postgresql://localhost:5431/eventapi
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true

  flyway:
    enabled: true